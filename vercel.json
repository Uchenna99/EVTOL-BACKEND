{
  "version": 2,
  "buildCommand": "npm run build && prisma generate",
  "builds": [
    { "src": "dist/Index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "dist/Index.js" }
  ]
}
